# CyberGym Risk Report

_deployment: **demo-prod** -- generated by `scripts/pipeline.py`_

## TL;DR

> **Patch `api_gateway` (service) first** — it sits on **12 / 15** live kill paths (**80%** attack-surface reduction).

- Threat intel: **3 / 8** ingested CVEs actually land on our inventory (5 not applicable).
- Live attack paths to critical assets: **15**.
- Top-1 chokepoint covers **80%** of paths.

## 1. Inventory at a glance

- nodes: **19**, edges: **52** (incl. VULN_EXPLOIT)
- per layer: `{'L0 Entry': 1, 'L1 Product': 2, 'L2 Service': 6, 'L3 Workload': 6, 'L4 InfraNode': 4}`
- per edge type: `{'NETWORK_REACH': 8, 'VULN_EXPLOIT': 19, 'BELONGS_TO': 6, 'RUNS_AS': 6, 'DEPENDS_ON': 6, 'IAM_BINDING': 4, 'DATA_FLOW': 3}`

## 2. Threat intel coverage

Base Model produced **8** capability entries. **3** of them found a matching inventory node and produced VULN_EXPLOIT edges; **5** are recorded but have no impact on this deployment.

### Applicable CVEs

| CVE / task | type | cvss | post_condition | edges | rationale |
|---|---|---:|---|---:|---|
| `syzbot-aabb1122` | InfraNode | 10.0 | rce | 6 | The description indicates kernel code execution from a type-confusion vulnerability, which can directly lead to remote c |
| `syzbot-ccdd3344` | InfraNode | 9.2 | escape_to_host, privilege_escalation, rce | 6 | Kernel double-free in codec subsystem allows local attacker with untrusted input to achieve RCE and privilege escalation |
| `10055` | Service | 7.5 | denial_of_service, rce | 2 | Use-after-free in OpenSSL's input parser on untrusted data can cause memory corruption leading to RCE or DoS; network-fa |

### Not applicable (no inventory match)

- `10013` (Workload) — looks for project `ffmpeg`, not in our inventory
- `CVE-2024-1939` (Service) — looks for project `chromium_v8`, not in our inventory
- `CVE-2024-6100` (Service) — looks for project `chromium_v8`, not in our inventory
- `10096` (Workload) — looks for project `libxml2`, not in our inventory
- `CVE-2024-10231` (Workload) — looks for project `chromium_v8`, not in our inventory

## 3. Top kill paths

Sorted by score = max(CVSS) x criticality x vuln_factor - depth_penalty.

| score | cvss | depth | target | path |
|---:|---:|---:|---|---|
| 21.9 | 10.0 | 3 | `node-data-01` | `internet -> user_db -> postgres-image -> node-data-01` |
| 21.9 | 10.0 | 3 | `node-edge-01` | `internet -> api_gateway -> api-gw-image -> node-edge-01` |
| 21.6 | 10.0 | 4 | `node-app-01` | `internet -> api_gateway -> order_svc -> order-app-image -> node-app-01` |
| 21.6 | 10.0 | 4 | `node-app-02` | `internet -> api_gateway -> search_svc -> search-app-image -> node-app-02` |
| 21.3 | 10.0 | 5 | `node-app-01` | `internet -> api_gateway -> order_svc -> payment_svc -> payment-app-image -> node-app-01` |
| 21.3 | 10.0 | 5 | `node-data-01` | `internet -> api_gateway -> order_svc -> user_db -> postgres-image -> node-data-01` |
| 21.3 | 10.0 | 5 | `node-data-01` | `internet -> api_gateway -> order_svc -> cache_redis -> redis-image -> node-data-01` |
| 16.88 | 7.5 | 1 | `api_gateway` | `internet -> api_gateway` |
| 16.88 | 7.5 | 1 | `user_db` | `internet -> user_db` |
| 16.57 | 7.5 | 2 | `order_svc` | `internet -> api_gateway -> order_svc` |
| 16.27 | 7.5 | 3 | `cache_redis` | `internet -> api_gateway -> order_svc -> cache_redis` |
| 16.27 | 7.5 | 3 | `payment_svc` | `internet -> api_gateway -> order_svc -> payment_svc` |

## 4. Recommended chokepoints

Patch / harden these nodes first. `paths_cut` is how many of the kill paths above pass through this node.

| rank | node | kind | paths_cut | score_cut | betweenness | blend |
|---:|---|---|---:|---:|---:|---:|
| 1 | `api_gateway` | service | 12 | 221.6 | 0.0392 | 0.7118 |
| 2 | `order_svc` | service | 7 | 134.31 | 0.0447 | 0.4217 |
| 3 | `user_db` | service | 2 | 43.2 | 0.03 | 0.1257 |
| 4 | `search_svc` | service | 2 | 37.87 | 0.0174 | 0.1219 |
| 5 | `postgres-image` | workload | 2 | 43.2 | 0.0169 | 0.1217 |

## 5. How to read `attack_graph.html`

Open it with `.\run.ps1 graph`. Page is split: 5-layer SVG topology on the left, 6 info panels on the right.

### Layers (top to bottom)

| layer | what it represents | how to spot |
|---|---|---|
| L0 Entry | `internet` synthetic node, attack origin | red box, very top |
| L1 Product | business unit (e.g. order system) | green box |
| L2 Service | microservice / DB | blue box |
| L3 Workload | container image / firmware | grey box |
| L4 InfraNode | host / VM | dark grey box, very bottom |

Attackers always enter from the top and try to reach L4. **L4 compromise = host pwn = worst case.**

### Edge colours

| colour | type | meaning |
|---|---|---|
| blue solid | NETWORK_REACH | A can reach B over the network |
| yellow dashed | IAM_BINDING | A has credentials/RBAC to call B |
| purple dotted | DATA_FLOW | A reads/writes B's data |
| green dashed | BELONGS_TO | service belongs to product (org link) |
| grey | RUNS_AS / DEPENDS_ON | service runs as image / image runs on host |
| **red bold** | **VULN_EXPLOIT** | **CVE lets attacker pivot from A to B** ← the ones that matter |

Without the red edges this is just an asset inventory. The red edges turn it into an *attack graph*.

### Right-side panels

1. **TL;DR headline** (top, red gradient): the one number SOC wants — patch X, kill Y% paths.
2. **Inventory + Threat Intel stats**: split deliberately. Inventory = what you own. Threat Intel = how many ingested CVEs actually land on it (`applicable to us`) vs are filed-away-but-irrelevant (`not applicable`).
3. **Top Chokepoints**: nodes ranked by `paths_cut × 0.7 + betweenness × 0.3`. Click a row to highlight the node yellow.
4. **Capability Table (CVE intel)**: every Base Model output. Each card shows CVE, type, CVSS, post-condition, and how many edges it injected. Cards with `[no match]` are dimmed (no inventory hit). Click an applicable card to highlight every red edge that CVE produced.
5. **Kill Paths**: live attack chains, ranked. Click a row to dye the whole chain red and dim everything else.
6. **Selected Node**: click any node in the SVG to see attributes + incident edges.

### Three click modes are mutually exclusive

Clicking a node, a kill path, or a capability each clears the others — so the highlight is always unambiguous. Click the same item again to deselect.

### A 6-step walkthrough

1. Read the **TL;DR** headline (3 seconds): which node should I patch, what's the win.
2. Glance at **Threat Intel stats**: `applicable to us` tells you how many of the ingested CVEs actually matter.
3. Open the **Capability Table** panel: applicable CVEs are the red bold cards; each is a real threat.
4. Click each applicable CVE card one by one to see *which* nodes it lights up red.
5. Click the top **Kill Path** to see the full attacker chain in red, with everything else dimmed.
6. Cross-reference with **Top Chokepoints**: the node that appears on the most paths is your patch priority.

## 6. Method note

1. Static baseline (NETWORK_REACH / IAM_BINDING / DATA_FLOW / RUNS_AS / DEPENDS_ON / BELONGS_TO) is loaded from `inventory/sample.yaml`.
2. Per-CVE capability tables are produced by the **Base Model** (`base_model/translate.py`) — an LLM constrained to a fixed JSON schema with vocabulary `affected_node_type / pre_condition / post_condition / cvss / exploit_maturity / match_hints`.
3. The **Harness** matches each capability to inventory nodes by `affected_node_type` and `match_hints.project_name`, then injects directional `VULN_EXPLOIT` edges. The injection algorithm is purely deterministic; the LLM never participates.
4. Kill paths are simple paths from `internet` to any critical asset, depth <= 6, walking `NETWORK_REACH | IAM_BINDING | RUNS_AS | DEPENDS_ON | VULN_EXPLOIT`.
5. Chokepoints blend path-cut count (70%) with betweenness centrality (30%) to surface the single nodes whose patch buys the most attack-surface reduction.